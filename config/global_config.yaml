storage:
  base_path: "data"
  date_format: "%Y-%m-%d"
  output_format: "org"  # 输出格式：'org' 或 'json' 或 'both'
  
logging:
  level: "INFO"
  file: "logs/crawler.log"
  max_size_mb: 10
  
scheduler:
  check_interval: 60  # 检查任务间隔（秒）
  max_workers: 5      # 最大并发数
  default_update_frequency: 120  # 默认更新频率（分钟），如果规则文件中未指定则使用此值

# 全局过滤器配置（可选）
# filters:
#   - type: "title"        # 标题关键字过滤
#     keywords: ["agent", "diffusion"]
#     negate: false        # 是否反选
#   - type: "summary"      # 摘要关键字过滤
#     keywords: ["LLM"]
#   - type: "author"       # 作者关键字过滤
#     keywords: ["Smith"]
#     negate: true         # 反选：排除包含这些作者的条目
#   - type: "time_range"   # 时间范围过滤
#     start: "2025-01-01"  # 或者使用 relative_days / relative_hours
#     end: "2025-01-31"

